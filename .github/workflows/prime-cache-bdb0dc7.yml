name: Cache Demo

on: [push]

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: prime-cache-bdb0dc7
      uses: actions/cache@v4
      with:
        path: ~/.cache/demo
        key: cache-bdb0dc7
    
    - name: Echo result
      run: echo "::notice:: Cache step ran (check logs for hit/miss)"
    
    - run: mkdir -p ~/.cache/demo && echo "data" > ~/.cache/demo/file.txt
